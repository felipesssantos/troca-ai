services:
  app:
    image: ghcr.io/${GH_REPO}:latest
    container_name: troca-ai-app
    ports:
      - "3001:80"
    restart: always

  landing:
    image: ghcr.io/${GH_REPO}:latest
    container_name: troca-ai-landing
    ports:
      - "3002:80"
    restart: always
